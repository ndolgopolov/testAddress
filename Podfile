# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'testAddress' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
   use_frameworks!

  # Pods for testAddress
  pod 'GoogleMaps'
  pod 'GooglePlaces'
  

  target 'testAddressTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'testAddressUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
